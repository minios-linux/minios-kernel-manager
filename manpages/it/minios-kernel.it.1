.\"*******************************************************************
.\"
.\" This file was generated with po4a. Translate the source file.
.\"
.\"*******************************************************************
.TH MINIOS\-KERNEL 1 "Settembre 2026" "Gestore kernel MiniOS" "Comandi utente"
.SH NOME
minios\-kernel \- Strumento a riga di comando per la gestione dei kernel
MiniOS
.SH SINTASSI
\fBminios\-kernel\fP [\fI\-\-help\fP]
.br
\fBminios\-kernel\fP [\fB\-\-json\fP] \fBlist\fP
.br
\fBminios\-kernel\fP [\fB\-\-json\fP] \fBactivate\fP \fIkernel\-version\fP
.br
\fBminios\-kernel\fP [\fB\-\-json\fP] \fBinfo\fP [\fIkernel\-version\fP]
.br
\fBminios\-kernel\fP [\fB\-\-json\fP] \fBpackage\fP {\fB\-\-repo\fP \fIpackage\fP |\fB\-\-deb\fP
\fIfile...\fP} \fB\-o\fP \fIoutput\-dir\fP [\fIoptions\fP]
.br
\fBminios\-kernel\fP [\fB\-\-json\fP] \fBstatus\fP
.br
\fBminios\-kernel\fP [\fB\-\-json\fP] \fBdelete\fP \fIkernel\-version\fP
.SH DESCRIZIONE
\fBminios\-kernel\fP è un'utilità a riga di comando per la gestione dei kernel
Linux nella distribuzione MiniOS. Richiede privilegi di root.
.PP
Questo strumento funge sia da utility CLI autonoma sia da backend per
l'interfaccia grafica \fBminios\-kernel\-manager\fP. Gestisce l'impacchettamento
dei kernel, l'attivazione e fornisce informazioni dettagliate sui kernel.
.SH OPZIONI
.TP
\fB\-h, \-\-help\fP
Mostra il messaggio di aiuto integrato ed esce.
.TP
\fB\-\-json\fP
Restituisce i risultati in formato JSON. Può essere utilizzato con qualsiasi
comando.
.SH COMANDI
.TP
\fBlist\fP
Elenca i kernel disponibili con il loro stato.
.TP
\fBactivate\fP\fI kernel\-version\fP
Attiva la versione di kernel specificata per il prossimo avvio.
.TP
\fBinfo\fP\fI [kernel\-version]\fP
Mostra informazioni su un kernel. Se non viene specificata una versione,
mostra il kernel attivo attualmente.
.TP
\fBpackage\fP
Impacchetta un kernel da un repository o da uno o più file .deb locali.
.TP
\fBstatus\fP
Verifica lo stato della directory MiniOS e i permessi di scrittura.
.TP
\fBdelete\fP\fI kernel\-version\fP
Elimina il kernel impacchettato specificato dal repository.
.SH "OPZIONI DEL PACCHETTO"
.TP
\fB\-\-repo\fP\fI nome\-pacchetto\fP
Scarica e crea un pacchetto kernel versionato reale dalla configurazione APT
del sistema esistente. I metapacchetti di tracciamento come
linux\-image\-amd64 non sono accettati.
.TP
\fB\-\-deb\fP\fI file\-deb...\fP
Crea un pacchetto kernel da uno o più file .deb locali. I kernel suddivisi
di Debian richiedono un insieme coerente di image, binary, base e modules; i
kernel suddivisi di Ubuntu richiedono i pacchetti image e modules
corrispondenti, oltre a modules\-extra se selezionato.
.TP
\fB\-o, \-\-output \fP\fIdirectory\-output\fP
Directory in cui salvare i file del kernel impacchettato. Obbligatoria.
.TP
\fB\-\-sqfs\-comp\fP\fI metodo\fP
Metodo di compressione per l'immagine SquashFS. Predefinito: \fBzstd\fP.
.TP
\fB\-\-temp\-dir\fP\fI directory\fP
Directory temporanea personalizzata (deve avere almeno 1024MB di spazio
libero).
.TP
\fB\-\-force\-update\fP
Esegue apt update prima dell'impacchettamento nel repository. Senza questa
opzione, vengono utilizzati gli elenchi dei pacchetti di sistema esistenti
senza verificare la loro età.
.TP
\fB\-\-dkms\-driver\fP\fI id\-driver\fP
Compila un driver aggiuntivo dal catalogo DKMS 01\-kernel selezionato. Questa
opzione può essere ripetuta ed è supportata solo con \-\-repo, dove le
intestazioni corrispondenti possono essere risolte automaticamente.
.SH IMBALLAGGIO
La modalità repository risolve automaticamente i pacchetti linux\-modules
corrispondenti. La modalità locale accetta più pacchetti per kernel
suddivisi. L'output include l'immagine del kernel, initramfs e il modulo
SquashFS; alcuni metadati dpkg selezionati vengono mantenuti all'interno di
SquashFS in \fI/usr/share/minios/kernel\-dpkg/\fP.
.PP
I driver DKMS selezionati vengono compilati in una chroot privata di overlay
senza modificare il database dei pacchetti di sistema in esecuzione. I
moduli risultanti vengono verificati rispetto alla versione del kernel di
destinazione prima dell'inclusione. La provenienza dei driver è memorizzata
in \fI/usr/share/minios/kernel\-dkms/\fP.
.PP
I download dal repository sono input temporanei dell'area di lavoro e
vengono rimossi dopo il successo o in caso di errore gestito. Gli archivi
locali rimangono di proprietà del chiamante. I nuovi bundle utilizzano il
formato congelato canonico 1 con un pacchetto immagine gestito manualmente,
pacchetti kernel subordinati automatici e senza configurazione repository
incorporata.
.PP
Il percorso canonico del modulo runtime è /lib/modules/<version>. Il
percorso fisico memorizzato nel bundle segue il sistema base in esecuzione:
lib/modules nei layout tradizionali oppure usr/lib/modules quando /lib è
usrmerged. L'impacchettamento rifiuta un albero di moduli della stessa
versione già esistente invece di mescolarlo con una revisione di pacchetto
diversa.
.PP
Quando viene rilevata una root MiniOS scrivibile e l'output non è già la
directory del repository, il pacchetto completato viene posizionato in modo
atomico in \fI<MiniOS\-root>/kernels/<version>\fP.
.PP
L'impacchettamento richiede un runtime live MiniOS con mkdracut o mkinitrfs
in \fI/run/initramfs\fP, oltre a mksquashfs, depmod e dpkg\-deb. La modalità
repository richiede anche APT.
.SH "PERSISTENZA CIFRATA"
Quando l'initrd in esecuzione segnala la persistenza cifrata, il kernel
sostitutivo deve fornire il supporto per dm\-crypt. L'initramfs generato
viene verificato per la presenza di cryptsetup, dm\-crypt.ko ed
etc/minios\-initramfs\-crypt. L'impacchettamento fallisce se tali protezioni
non possono essere garantite.
.SH ATTIVAZIONE
L'attivazione richiede una voce completa nel repository contenente SquashFS,
immagine del kernel e initramfs. I file di avvio e la configurazione del
bootloader vengono aggiornati in modo atomico e ripristinati in caso di
errore di attivazione. È richiesta la configurazione di GRUB; la
configurazione di SYSLINUX viene aggiornata se presente. L'attivazione
influisce sul prossimo avvio e non riavvia la macchina.
.PP
L'attivazione accetta bundle congelati in formato\-1 prodotti dal Manager e
bundle tracciati canonici prodotti da 01\-kernel. Il formato metadati legacy
1.2.1 è accettato solo dal percorso di compatibilità di attivazione live.
.SH ESEMPI
Elenca i kernel disponibili:
.RS
\fBsudo minios\-kernel list\fP
.RE
.PP
Attiva uno specifico kernel:
.RS
\fBsudo minios\-kernel activate 6.12.38+deb13\-amd64\fP
.RE
.PP
Crea un pacchetto kernel dal repository:
.RS
\fBsudo minios\-kernel package \-\-repo linux\-image\-6.12.38+deb13\-amd64 \-o /tmp/output\fP
.RE
.PP
Crea un pacchetto kernel dal repository con due driver aggiuntivi:
.RS
\fBsudo minios\-kernel package \-\-repo linux\-image\-6.12.38+deb13\-amd64 \-\-dkms\-driver dynblk \-\-dkms\-driver rtl8821au \-o /tmp/output\fP
.RE
.PP
Crea un pacchetto kernel da un file .deb locale:
.RS
\fBsudo minios\-kernel package \-\-deb kernel.deb \-o /tmp/output\fP
.RE
.PP
Elimina un kernel impacchettato:
.RS
\fBsudo minios\-kernel delete 6.12.38+deb13\-amd64\fP
.RE
.SH "USCITA JSON"
Quando si utilizza l'opzione \fB\-\-json\fP, i comandi restituiscono dati
strutturati:
.TP
\fBlist\fP
Restituisce un array di kernel con versione, stato e metadati.
.TP
\fBattiva\fP
Restituisce lo stato di successo e i dettagli dell'attivazione.
.TP
\fBinfo\fP
Restituisce le informazioni sul kernel, inclusi versione, stato e percorsi
di sistema.
.TP
\fBpackage\fP
Restituisce aggiornamenti sul progresso durante il packaging e lo stato
finale di successo.
.TP
\fBstatus\fP
Restituisce lo stato della directory MiniOS e informazioni sul filesystem.
.TP
\fBelimina\fP
Restituisce lo stato di successo e i dettagli dell'eliminazione.
.SH FILE
.TP
\fI/run/initramfs/memory/data/minios/\fP
Root MiniOS preferito nell'ambiente di esecuzione standard.
.TP
\fI/lib/live/mount/medium/minios/\fP
Root MiniOS di fallback su un supporto live montato.
.TP
\fI<MiniOS\-root>/kernels/<version>/\fP
Repository per kernel impacchettati inattivi.
.TP
\fI<MiniOS\-root>/boot/vmlinuz\-*\fP
Immagine kernel attiva.
.TP
\fI<MiniOS\-root>/boot/initrfs\-*.img\fP
Immagine initramfs attiva.
.TP
\fI<MiniOS\-root>/01\-kernel\-*.sb\fP
Moduli kernel SquashFS attivi.
.TP
\fI<MiniOS\-root>/boot/active\-kernel\fP
Indicatore di versione attiva.
.SH AUTORE
crims0n <crims0n@minios.dev>
.SH "VEDERE ANCHE"
\fBminios\-kernel\-manager\fP(1), \fBmksquashfs\fP(1)
